front end application made for college purposes, used Nexst.JS TailwindCSS and shadcn components

To run in local install dependencies (Mysql, node and npm) and run npm run dev together with back end
